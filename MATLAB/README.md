This implementation of the **Metrics** package is compatible with both **MATLAB** and **Octave**

To install, first clone the repo:
```
git clone https://github.com/benhamner/Metrics.git
```

Then, open up MATLAB or Octave and execute the following from the prompt:
```
cd Metrics/MATLAB
setup
```

This will add the necessary functions to your path.
